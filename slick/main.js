$('document').ready(() => {
    let rightCarousel = $('main .main__container .right-bar .right-bar__top .right-carousel');
    $('.right-carousel').slick({
        arrows: false,
        dots: true
    })
});